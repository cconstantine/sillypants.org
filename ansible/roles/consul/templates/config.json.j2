{
    "bootstrap_expect": {{ groups.control|length }},
    "server": true,
    "datacenter": "us-west-2",
    "data_dir": "/var/consul",
    "advertise_addr": "{{ansible_eth0.ipv4.address}}",
    "ports": {
        "dns": 53
    },
    "addresses": {
        "dns": "{{ansible_eth0.ipv4.address}}"
    },
    "retry_join": [ "{{ hostvars[groups.control[(groups.control.index(inventory_hostname) + 1) % groups.control|length ]].ansible_eth0.ipv4.address }}" ]
}
