{
    "bootstrap_expect": {{ groups['swarm']|length }},
    "server": true,
    "datacenter": "us-west-2",
    "data_dir": "/var/consul",
    "advertise_addr": "{{ansible_eth0.ipv4.address}}",
    "client_addr": "0.0.0.0"{% if inventory_hostname != groups['swarm'][0] %},
    "start_join": [ "{{ hostvars[groups['swarm'][0]].ansible_eth0.ipv4.address }}" ]
{% endif %}
}
